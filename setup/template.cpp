#include <iostream>
#include <algorithm>
using namespace std;


int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    #ifndef ONLINE_JUDGE
    freopen("./template.INP", "r", stdin);
    freopen("./template.OUT", "w", stdout);
    #endif

    

    return 0;
}